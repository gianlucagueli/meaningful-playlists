services:
  redis:
    image: redis:7-alpine
    container_name: redis_mp_local
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    command: ["redis-server", "--requirepass", "root"]
    restart: unless-stopped

volumes:
  redis-data: